# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Nix / direnv / devenv
.direnv/
.devenv/
# Store symlink written by devenv's git-hooks integration on shell entry.
.pre-commit-config.yaml

# Editor / OS
*.local
.DS_Store
*.log

# Test / coverage
coverage/
.vitest-attachments/
__screenshots__/
# Ephemeral conformance dump (CONF_DUMP) written by the conformance test in CI.
conformance.json
# Ephemeral WS4 differential-fuzz report (fuzz:run task / weekly CI job).
fuzz-report.md
